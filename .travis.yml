language: node_js
node_js:
  - lts
cache:
  directories:
    - ~/.npm
node_js:
  - '10'
script:
  - yarn ci-lint
  - yarn test
  - yarn build
  - yarn build-demo
after_success:
  - yarn travis-deploy-once "yarn semantic-release"
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
